# IP of the Docker host, used as a fallback link for containers with a
# published port but no Traefik Host() label.
HOST_IP=192.168.1.100
# LAN subnet allowed to reach the dashboard through the Traefik ipallowlist middleware.
LAN_SUBNET=192.168.1.0/24
